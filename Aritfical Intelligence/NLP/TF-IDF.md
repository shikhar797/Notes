TF-IDF (Term Frequency-Inverse Document Frequency) vectorization is
==a Natural Language Processing (NLP) technique that converts text into numerical vectors based on word importance==. 
It calculates how relevant a term is to a document within a corpus, 
favoring words frequent in a specific document but rare across all documents.

![[Difference in CountVectorizer and TF-IDF.png]]

